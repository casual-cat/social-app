apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
spec:
  type: LoadBalancer
  selector:
    app: flask-app        # must match your Deployment labels
  ports:
    - port: 80           # the external port
      targetPort: 5001   # port inside the container (Flask)
